version: '3'
services:
  mysqldb:
    image: mysql:5.7
    restart: always
    container_name: mysql
    volumes:
      - data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD:
      MYSQL_DATABASE: atviras-seimas
      MYSQL_USER: dev
      MYSQL_PASSWORD: root9198
    ports:
      - 3306:3306
volumes:
  data:
